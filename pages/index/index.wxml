<view class="main">
    <view wx:for="{{infos}}" wx:key="_id" class="info">
        <text>
            {{item.text}}
        </text>
        <button bindtap="remove" data-id="{{item._id}}">删除</button>
    </view>
</view>